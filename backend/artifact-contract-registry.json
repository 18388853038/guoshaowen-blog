{
  "contracts": [
    {
      "id": "ceo_to_agent_task",
      "purpose": "CEO 分配任务给子 Agent 时的产物格式",
      "format": "json",
      "schema": {
        "type": "object",
        "required": ["taskName", "objective", "assignedTo"],
        "properties": {
          "taskName": { "type": "string" },
          "objective": { "type": "string" },
          "assignedTo": { "type": "string" },
          "deadline": { "type": "string" },
          "outputFormat": { "type": "string" }
        }
      },
      "producer": "CEO",
      "consumer": "staff",
      "requiredFields": ["taskName", "objective", "assignedTo"],
      "createdAt": "2026-06-10T06:00:00.000Z",
      "updatedAt": "2026-06-10T06:00:00.000Z"
    }
  ]
}

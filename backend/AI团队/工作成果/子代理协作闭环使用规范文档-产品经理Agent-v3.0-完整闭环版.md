---
# 写入者: 产品经理Agent (pm-agent)
# 写入时间: 2026-06-18 11:20:48
---

---
# 写入者: 产品经理Agent (pm-agent)
# 写入时间: 2026/06/18 19:19:59
---

# 子代理协作闭环使用规范文档（完整闭环版 v3.0）

> **文档版本**：v3.0（完整闭环版）
> **编写人**：产品经理Agent
> **编写日期**：2026/06/18 19:19
> **审核状态**：✅ 已完成，待小龙主Agent终审归档
> **优先级**：🔴 加急
> **适用范围**：小龙主Agent调度CTO/安全审计/产品经理/工程师子代理的标准流程

---

## 📋 文档概述

本文档规范了**小龙主Agent**调度各子代理（CTO、安全审计、产品经理、工程师等）完成任务的**标准协作闭环流程**，涵盖任务分配、执行追踪、成果审核、闭环归档四大环节，确保子代理协作高效、可追溯、质量可控。

### 核心原则

| 原则 | 说明 |
|:----|:----|
| **主控调度** | 小龙主Agent统一分配任务，子代理不越权 |
| **闭环必达** | 每项任务完成后必须调用 `complete_claimed_task` 回调 |
| **成果物化** | 所有产出必须通过 `write_file` 写入 `AI团队/工作成果/` 目录 |
| **知识沉淀** | 踩坑经验通过 `add_pitfall` 记录到避坑指南 |
| **可追溯性** | 每个任务从领取到完成形成完整链路记录 |

---

## 1. 角色定义与职责矩阵

| 角色 | 身份 | 核心职责 | 核心工具 | 产出物类型 |
|:----:|------|----------|:--------:|:----------:|
| 🧑‍💼 **小龙（主Agent）** | 总调度/CEO | 任务下发、进度追踪、成果终审、闭环决策 | 任务系统、回调链 | 汇总报告、状态表 |
| 👨‍💻 **CTO子代理** | 技术负责人 | 技术方案设计、代码审查、架构决策、技术风险把控 | `code_review` `api_dev` `performance_opt` | 技术方案、架构文档、审查报告 |
| 🔒 **安全审计子代理** | 安全负责人 | 安全漏洞扫描、权限审计、合规检查、安全加固 | `security_audit` `code_review` | 安全审计报告、漏洞清单、修复建议 |
| 📋 **产品经理子代理** | 产品负责人 | 需求分析、PRD编写、竞品调研、用户故事、路线图规划 | `req_analysis` `requirement_doc` `user_story` `competition_analysis` | PRD、需求文档、竞品分析报告 |
| 👷 **工程师子代理** | 执行者 | 代码开发、测试执行、Bug修复、部署运维 | 各类开发工具 | 代码、测试报告、部署记录 |

---

## 2. 全流程闭环模型

```
┌─────────────────────────────────────────────────────────────────────┐
│                    子代理协作闭环全流程                               │
│                                                                     │
│  ① 任务分配 ──→ ② 子代理执行 ──→ ③ 成果提交 ──→ ④ 小龙审核        │
│      ↑                                              │              │
│      └────────── ⑤ 归档/返工 ←──── ④ 审核不通过 ────┘              │
│                                                                     │
│  ① = 小龙主Agent创建任务，分配至对应子代理                           │
│  ② = 子代理通过 check_pending_tasks → claim_task 领取并执行         │
│  ③ = 子代理 write_file 写入成果 → complete_claimed_task 回调        │
│  ④ = 小龙主Agent审核成果（通过/驳回）                               │
│  ⑤ = 通过→归档闭环；驳回→返回②修订                                 │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. 任务分配规范

### 3.1 任务创建标准

小龙主Agent在分配任务时，必须包含以下信息：

| 字段 | 必填 | 说明 |
|:----:|:----:|------|
| **任务标题** | ✅ | 清晰描述任务内容，如「【加急】P95延迟过高排查」 |
| **任务描述** | ✅ | 详细说明任务目标、背景、交付物要求 |
| **目标子代理** | ✅ | 指定由哪个子代理执行（CTO/安全审计/产品经理/工程师） |
| **优先级** | ✅ | P0-紧急 / P1-高 / P2-中 / P3-低 |
| **截止时间** | ✅ | 明确交付截止日期 |
| **依赖关系** | 可选 | 前置任务或关联任务说明 |
| **参考材料** | 可选 | 相关文档、代码、数据等参考链接 |

### 3.2 任务分配流程

```
小龙主Agent → 创建任务 → 任务系统
                              ↓
子代理激活 → check_pending_tasks（检查待办）
                              ↓
                    claim_task（领取任务）
                              ↓
                    执行任务 → write_file（写入成果）
                              ↓
                    complete_claimed_task（回调提交）
                              ↓
                    小龙主Agent审核 → 归档/驳回
```

### 3.3 任务分配原则

1. **单一职责原则**：一个任务只分配给一个子代理，避免多头负责
2. **能力匹配原则**：根据子代理的专业技能分配对应类型任务
3. **负载均衡原则**：避免同一子代理同时承担过多高优任务
4. **依赖有序原则**：有依赖关系的任务按顺序分配

---

## 4. 子代理执行规范

### 4.1 子代理标准执行闭环

每个子代理在被激活后，必须严格遵循以下工作闭环：

```
┌─────────────────────────────────────────────┐
│          子代理工作闭环（5步法）              │
│                                             │
│  STEP 1: check_pending_tasks                │
│    ← 每次激活第一件事，先查待办任务           │
│         ↓                                   │
│  STEP 2: claim_task                         │
│    ← 有任务就领，系统自动分配最高优先级       │
│         ↓                                   │
│  STEP 3: 执行任务                            │
│    ← 使用 read_file / 专业工具完成工作        │
│         ↓                                   │
│  STEP 4: write_file                         │
│    ← 必须将成果写入 AI团队/工作成果/ 目录     │
│         ↓                                   │
│  STEP 5: complete_claimed_task              │
│    ← 必须回调，否则主Agent收不到通知          │
│         ↓                                   │
│  回到 STEP 1，循环检查下一个任务              │
└─────────────────────────────────────────────┘
```

### 4.2 各子代理执行规范详情

#### 4.2.1 CTO子代理执行规范

| 环节 | 规范要求 |
|:----:|----------|
| **执行工具** | `code_review` `api_dev` `performance_opt` `architecture_design` |
| **交付物** | 技术方案文档、代码审查报告、架构设计图、性能优化方案 |
| **文件命名** | `{任务主题}-CTO子代理-v{版本}.md` |
| **存储路径** | `AI团队/工作成果/` |
| **质量标准** | 方案需包含：背景分析、方案对比、技术选型、风险评估、实施计划 |

#### 4.2.2 安全审计子代理执行规范

| 环节 | 规范要求 |
|:----:|----------|
| **执行工具** | `security_audit` `code_review` 安全扫描工具 |
| **交付物** | 安全审计报告、漏洞清单、修复建议、合规检查报告 |
| **文件命名** | `安全审计-{任务主题}-安全审计Agent-v{版本}.md` |
| **存储路径** | `AI团队/工作成果/` |
| **质量标准** | 报告需包含：审计范围、发现项、风险等级、修复建议、复测结果 |

#### 4.2.3 产品经理子代理执行规范

| 环节 | 规范要求 |
|:----:|----------|
| **执行工具** | `req_analysis` `requirement_doc` `user_story` `competition_analysis` `roadmap_plan` |
| **交付物** | PRD文档、需求规格说明书、用户故事地图、竞品分析报告、路线图 |
| **文件命名** | `PRD-{项目名}-产品经理Agent-v{版本}.md` 或 `{任务主题}-产品经理Agent.md` |
| **存储路径** | `AI团队/工作成果/` |
| **质量标准** | PRD需包含：背景目标、用户故事、功能规格、验收标准、优先级排序 |

#### 4.2.4 工程师子代理执行规范

| 环节 | 规范要求 |
|:----:|----------|
| **执行工具** | 代码开发、测试、部署相关工具 |
| **交付物** | 代码文件、测试报告、部署文档 |
| **文件命名** | `{项目名}-工程师子代理-执行报告-v{版本}.md` |
| **存储路径** | `AI团队/工作成果/` |
| **质量标准** | 代码需通过审查、测试覆盖关键路径、部署文档完整 |

### 4.3 执行红线（禁止行为）

| ❌ 禁止行为 | 说明 |
|:----------:|------|
| **拒绝调用write_file** | 子代理必须将成果写入文件系统，不得仅口头回复 |
| **跨角色越权执行** | 非本专业任务应拒绝，由小龙主Agent重新分配 |
| **跳过complete_claimed_task** | 完成任务后必须回调，否则任务链断裂，CEO收不到通知 |
| **超时未响应且不说明原因** | 超过截止时间未完成需主动说明原因 |
| **文件写入错误目录** | 所有成果必须写入 `AI团队/工作成果/` 目录 |

---

## 5. 成果提交规范

### 5.1 提交触发

子代理完成执行后，必须调用 `complete_claimed_task` 工具，触发自动回调链：

```
complete_claimed_task
    ↓
自动写执行日志 → 通知CEO(小龙主Agent) → 记入记忆库 → 标记空闲 → 检查下一个任务
```

### 5.2 提交参数规范

| 参数 | 必填 | 填写规范 |
|:----:|:----:|----------|
| **taskId** | ✅ | 领取任务时获得的任务ID，必须正确填写 |
| **result** | ✅ | 执行结果摘要，格式：「完成了XXX，产出了XXX文件，核心结论是XXX」 |
| **success** | ✅ | true=成功完成；false=执行失败需在result中说明原因 |

### 5.3 提交示例

```json
{
  "taskId": "task_20260618_001",
  "result": "完成了P95延迟过高排查任务，产出了AI团队/工作成果/P95延迟排查-产品经理Agent-v1.0.md，核心结论是非系统故障（健康分99/错误率0%/0.3QPS），根因是DeepSeek模型Token生成耗时",
  "success": true
}
```

### 5.4 成果文件头部元信息规范

每份产出文档必须包含以下头部信息：

```markdown
---
# 写入者: {角色名} ({角色英文名})
# 写入时间: {当前时间}
---

> **编写者**：{角色名}
> **编写日期**：{当前时间}
> **所属公司**：eCompany-Claw · AI 虚拟公司（V3.0）
> **关联任务**：{任务标题或ID}
```

---

## 6. 审核闭环规范

### 6.1 审核流程

```
小龙主Agent收到 complete_claimed_task 回调
    ↓
读取子代理产出的文件进行审核
    ↓
┌── 审核通过？──┐
│               │
是              否
│               │
↓               ↓
归档闭环        驳回返工
                    │
                    ↓
               子代理重新执行
               重新提交成果
               重新审核
```

### 6.2 审核标准

| 审核维度 | 检查项 | 通过条件 |
|:--------:|--------|:--------:|
| **完整性** | 交付物是否包含所有必填内容 | 无遗漏项 |
| **正确性** | 技术/业务判断是否准确 | 无事实错误 |
| **规范性** | 文件命名、格式是否符合规范 | 符合规范要求 |
| **可读性** | 内容是否清晰、结构化 | 便于后续查阅 |
| **可执行性** | 方案/建议是否具备落地条件 | 有明确实施路径 |

### 6.3 归档要求

审核通过后，小龙主Agent需完成以下动作：

1. **确认归档**：在任务系统中标记任务为「已完成」
2. **记录经验**：将关键经验写入避坑指南（`add_pitfall`）
3. **更新知识库**：将可复用的知识更新到团队知识库
4. **通知相关方**：如有需要，通知关联任务的子代理

---

## 7. 异常处理机制

### 7.1 子代理超时/卡住

| 场景 | 处理方式 |
|:----:|----------|
| 子代理超过截止时间未完成 | 小龙主Agent发送催办通知，同时评估是否需要重新分配 |
| 子代理连续3次未响应 | 标记为异常状态，由小龙主Agent介入排查 |
| 子代理执行失败 | 在 `complete_claimed_task` 中设置 `success=false` 并说明原因 |
| 工具调用异常 | 记录到避坑指南，尝试替代方案 |

### 7.2 任务冲突

| 场景 | 处理方式 |
|:----:|----------|
| 同一子代理被分配2个以上P0任务 | 子代理主动向小龙主Agent汇报，协商优先级 |
| 多个子代理需要协作 | 小龙主Agent创建主任务+子任务，分别分配 |
| 任务依赖链断裂 | 小龙主Agent介入协调，调整任务顺序 |

### 7.3 成果质量不达标

| 场景 | 处理方式 |
|:----:|----------|
| **首次驳回** | 小龙主Agent明确驳回原因和改进要求 |
| **二次驳回** | 小龙主Agent介入指导或重新分配 |
| **三次驳回** | 升级处理，由小龙主Agent直接完成或更换执行人 |

### 7.4 系统重试机制

当子代理任务卡住时，系统会自动触发重试通知：

```
系统检测到任务卡住 → 发送【重试通知】 → 子代理收到后立即执行
    ↓                                                       ↓
连续3次重试仍失败 ← 子代理执行失败 ← 子代理尝试执行
    ↓
小龙主Agent接管任务，手动处理
```

---

## 8. 全链路追踪机制

### 8.1 任务状态流转

```
待领取 ──→ 执行中 ──→ 已提交 ──→ 审核中 ──→ 已完成（通过）
                                      ──→ 已驳回（返回执行中）
```

### 8.2 追踪方式

| 追踪方式 | 说明 | 频率 |
|:--------:|------|:----:|
| **check_pending_tasks** | 子代理自查待办任务 | 每次激活时 |
| **complete_claimed_task回调** | 自动通知小龙主Agent | 每次提交时 |
| **小龙主Agent巡检** | 主动检查所有子代理任务进度 | 定时/按需 |

### 8.3 日志记录

每次任务闭环后，自动记录以下信息到团队记忆库：

- 任务ID与标题
- 执行子代理
- 产出文件路径
- 审核结论
- 关键经验/教训

---

## 9. 文件命名与存放规范

### 9.1 命名格式

```
{任务主题}-{子代理角色名}(-v{版本}).md
```

### 9.2 命名示例

| 任务主题 | 子代理 | 文件名 |
|:----|:----|:----|
| P95延迟过高排查与优化 | CTO子代理 | `P95延迟过高排查与优化-CTO子代理.md` |
| 安全合规审查 | 安全审计Agent | `安全合规审查-安全审计Agent.md` |
| 用户需求分析 | 产品经理Agent | `用户需求分析-产品经理Agent.md` |
| 子代理协作闭环使用规范文档 | 产品经理Agent | `子代理协作闭环使用规范文档-产品经理Agent-最终版.md` |

### 9.3 存放路径

所有文件统一存放于：**`AI团队/工作成果/`** 目录下

---

## 10. 文档模板标准

### 10.1 产品经理文档结构

```markdown
# {文档标题}
## 1. 概述
## 2. 需求分析
## 3. 用户故事
## 4. 功能规格
## 5. 优先级排序
## 6. 附录
```

### 10.2 技术文档结构

```markdown
# {文档标题}
## 1. 背景
## 2. 技术方案
## 3. 架构设计
## 4. 实施计划
## 5. 风险评估
## 6. 附录
```

### 10.3 安全审计文档结构

```markdown
# {文档标题}
## 1. 审计范围
## 2. 发现项
## 3. 风险评级
## 4. 修复建议
## 5. 合规检查
## 6. 附录
```

---

## 11. 知识沉淀规范

### 11.1 经验记录

子代理在完成任务过程中发现的经验教训，应通过以下方式沉淀：

| 工具 | 用途 | 使用时机 |
|:----:|:----:|:--------:|
| `add_pitfall` | 记录踩坑经验 | 发现坑时立即记录 |
| `query_pitfalls` | 新任务前查避坑指南 | 开始任务前 |
| `query_experience` | 搜索同事经验 | 遇到类似问题时 |
| `query_knowledge` | 查询技术知识 | 需要技术参考时 |

### 11.2 避坑指南记录格式

```python
add_pitfall(
    title="坑的标题（简明扼要）",
    content="踩坑详情：发生了什么、根因是什么",
    solution="解决方案/避免方法",
    severity="high"  # high=宕机/数据丢失, mid=功能异常, low=体验问题
)
```

---

## 12. 快速参考卡片

### 12.1 子代理工具速查

| 子代理 | 核心工具 | 产出物类型 |
|:------:|:--------:|:----------:|
| **CTO** | `code_review` `api_dev` `performance_opt` | 技术方案、审查报告 |
| **安全审计** | `security_audit` `code_review` | 安全报告、漏洞清单 |
| **产品经理** | `req_analysis` `requirement_doc` `user_story` `competition_analysis` | PRD、需求文档、竞品分析 |
| **工程师** | 各类开发工具 | 代码、测试报告 |

### 12.2 关键工具调用速查

```python
# 子代理检查任务
check_pending_tasks(limit=5)

# 子代理领取任务
claim_task(taskId="task_xxx")

# 子代理完成任务（必须调用）
complete_claimed_task(
    taskId="task_xxx",
    result="完成了XXX任务，产出了XXX文件，核心结论是XXX",
    success=True
)

# 写入成果文件
write_file(
    filepath="AI团队/工作成果/{任务主题}-{子代理角色}.md",
    content="# 文档内容..."
)

# 记录踩坑经验
add_pitfall(
    title="XXX问题",
    content="踩坑详情",
    solution="解决方案",
    severity="mid"
)
```

---

## 13. 实际案例参考

### 案例一：P95延迟过高排查与优化

| 环节 | 实际执行情况 |
|:----|:------------|
| **任务** | P95延迟过高排查（7809ms/阈值5000ms） |
| **执行子代理** | CTO子代理 + 安全审计Agent + 产品经理Agent |
| **执行过程** | 系统健康检查→性能分析→安全审计→根因定位→方案输出 |
| **产出文件** | 7份成果文件，覆盖排查全链路 |
| **核心结论** | 非系统故障（健康分99/错误率0%），根因为DeepSeek模型Token生成耗时 |
| **闭环状态** | ✅ 已归档闭环 |

### 案例二：微信桥接进程异常恢复

| 环节 | 实际执行情况 |
|:----|:------------|
| **任务** | 微信桥接进程异常恢复 |
| **执行子代理** | CTO子代理 + 安全审计Agent |
| **执行过程** | 技术排查→安全审计→修复方案→执行恢复→闭环确认 |
| **产出文件** | 多份排查报告、审计报告、执行报告 |
| **核心结论** | 进程异常已修复，系统已恢复正常运行 |
| **闭环状态** | ✅ 已归档闭环 |

---

## 14. 版本历史

| 版本 | 日期 | 更新内容 | 编写人 |
|:---:|:----:|:--------|:------:|
| v1.0 | 2026/06/18 | 初版编写 | 产品经理Agent |
| v2.0 | 2026/06/18 | 完善各子代理执行规范、审核闭环、异常处理 | 产品经理Agent |
| **v3.0** | **2026/06/18 19:19** | **完整闭环版，增加实际案例参考、快速参考卡片、版本历史、全量内容覆盖** | **产品经理Agent** |

---

## 15. 闭环确认清单

> 使用以下清单确认文档是否完整：

| 检查项 | 状态 | 说明 |
|:------|:----:|:----:|
| 角色定义与职责矩阵 | ✅ | 5个角色定义清晰 |
| 全流程闭环模型 | ✅ | 5步闭环流程 |
| 任务分配规范 | ✅ | 7个必填字段+4项原则 |
| 子代理执行规范 | ✅ | 5步法+4个子代理规范+5条红线 |
| 成果提交规范 | ✅ | 提交参数+示例+元信息规范 |
| 审核闭环规范 | ✅ | 审核流程+5维标准+归档要求 |
| 异常处理机制 | ✅ | 4类异常场景处理方案 |
| 全链路追踪机制 | ✅ | 状态流转+3种追踪方式 |
| 文件命名与存放规范 | ✅ | 命名格式+示例+路径 |
| 文档模板标准 | ✅ | 3类文档模板 |
| 知识沉淀规范 | ✅ | 4种沉淀工具+记录格式 |
| 快速参考卡片 | ✅ | 工具速查+代码速查 |
| 实际案例参考 | ✅ | 2个真实案例 |
| 版本历史 | ✅ | 3个版本记录 |

---

*本规范文档由产品经理Agent编写，已归档至 `AI团队/工作成果/` 目录。*
*编写时间：2026/06/18 19:19:59 Asia/Shanghai*
